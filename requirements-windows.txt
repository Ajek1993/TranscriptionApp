# =============================================================================
# REQUIREMENTS FOR WINDOWS (Native Installation)
# =============================================================================
# Ten plik zawiera zależności dla instalacji natywnej na Windows.
# Dla Dockera użyj: requirements.txt
# =============================================================================

# Core dependencies
yt-dlp>=2024.01.01
tqdm>=4.66.0
deep-translator>=1.11.0
edge-tts>=7.0.0
openai-whisper>=20240930

# =============================================================================
# GPU Support - CUDA 12.x (Windows)
# =============================================================================
# Zainstaluj PyTorch z CUDA 12.4 (kompatybilny z CUDA 12.1-12.8):
#
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124 --no-cache-dir
#
# =============================================================================
# Wymagania dla GPU:
# - NVIDIA GPU Driver >= 550.54
# - CUDA Toolkit 12.x (opcjonalnie, PyTorch zawiera biblioteki CUDA)
# =============================================================================
# Szczegółowa instrukcja konfiguracji GPU: CUDA_CONFIG.md
