# =============================================================================
# REQUIREMENTS FOR WINDOWS (Native Installation)
# =============================================================================
# Ten plik zawiera zależności dla instalacji natywnej na Windows.
# Dla Dockera użyj: requirements.txt
# =============================================================================

# Core dependencies
yt-dlp>=2024.12.01
tqdm>=4.67.0
deep-translator>=1.11.4
edge-tts>=7.0.0
openai-whisper>=20240930
langdetect>=1.0.9

# Coqui TTS - optional high-quality local TTS (requires GPU for best performance)
TTS>=0.22.0

# Transformers - PINNED for WhisperX compatibility
# WhisperX requires transformers with Pipeline API
transformers==4.48.0

# WhisperX - advanced transcription with word-level alignment
whisperx>=3.1.1,<4.0.0

# =============================================================================
# GPU Support - CUDA 12.1 (Windows) - MATCHED TO DOCKER
# =============================================================================
# Zainstaluj PyTorch 2.3.1 z CUDA 12.1 (identyczne jak Docker):
#
# pip install torch==2.3.1 torchaudio==2.3.1 --index-url https://download.pytorch.org/whl/cu121 --no-cache-dir
#
# NOTE: Bez torchvision (nie jest używane w projekcie, tak samo jak Docker)
# =============================================================================
# Wymagania dla GPU:
# - NVIDIA GPU Driver >= 527.41 (dla CUDA 12.1)
# - Python 3.11 (zalecane)
# - CUDA Toolkit 12.x (opcjonalnie, PyTorch zawiera biblioteki CUDA)
# =============================================================================
# Szczegółowa instrukcja konfiguracji GPU: CUDA_CONFIG.md
